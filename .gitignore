# ============================================
# Radio Monitor - Git Ignore Configuration
# ============================================
# This file ensures sensitive and temporary files are NOT committed to GitHub
# When adding new file types that should be ignored, add them to the appropriate section
# ============================================

# ------------------------------------------
# Python Generated Files
# ------------------------------------------
__pycache__/           # Compiled Python bytecode
*.py[cod]              # Compiled .pyc, .pyo files
*$py.class             # Compiled Java class files (if using Jython)
*.so                   # Compiled shared libraries
.Python                # Python build directories
*.egg-info/            # Python package metadata
build/                 # Python build directory (not dist/ - we need that for releases)
.pytest_cache/         # Pytest cache directory

# ------------------------------------------
# Testing Coverage Reports
# ------------------------------------------
.coverage              # Coverage.py data file
htmlcov/               # Coverage HTML reports

# ------------------------------------------
# Log Files
# ------------------------------------------
*.log                  # All application logs (radio_monitor.log, gui.log, etc.)

# ------------------------------------------
# Database Files (CRITICAL - Contains user data!)
# ------------------------------------------
*.db                   # SQLite database files (radio_songs.db, etc.)
*.db-shm               # SQLite shared-memory files
*.db-wal               # SQLite write-ahead logs
backups/               # Database backup directory

# ------------------------------------------
# User-Customizable Files (User-specific, should not be committed)
# ------------------------------------------
user_mappings.json     # User-defined artist name mappings (customized per installation)
# Note: user_mappings.json.example is NOT ignored (included in builds)

# ------------------------------------------
# IDE and Editor Files
# ------------------------------------------
.vscode/               # Visual Studio Code settings
.idea/                 # JetBrains IDE settings (PyCharm, etc.)
*.swp                  # Vim swap files
*.swo                  # Vim swap files
*~                     # Emacs backup files

# ------------------------------------------
# Operating System Files
# ------------------------------------------
.DS_Store              # macOS metadata files
Thumbs.db              # Windows thumbnail cache
desktop.ini            # Windows desktop settings

# ------------------------------------------
# Claude Code AI Memory (Internal tool files)
# ------------------------------------------
.claude/               # Claude Code memory and context

# ------------------------------------------
# Secrets and API Keys (CRITICAL - Never commit!)
# ------------------------------------------
*.secret_key.txt       # Generic secret key files
lidarr_api_key.txt     # Lidarr API keys
*_api_key.txt          # Any API key files
*_token.txt            # Any token files
.env                   # Environment variables with secrets
.secrets               # Secret configuration files

# ------------------------------------------
# Authentication and Configuration Files (Contains credentials!)
# ------------------------------------------
radio_monitor_auth.json      # User authentication credentials
radio_monitor_settings.json  # Application settings (contains API keys/tokens)

# ------------------------------------------
# Backup and Temporary Files
# ------------------------------------------
*.bak                  # Backup files
*.tmp                  # Temporary files
*.backup*              # Any backup files
*.backup_*             # Backup files with timestamp
*.old                  # Old versions of files
*~                     # Editor backup files
nul                    # Windows null device redirects

# ------------------------------------------
# Build and Distribution (We include dist/ for releases)
# ------------------------------------------
# Note: Only ZIP files in dist/pyinstaller/ are committed
# Individual build artifacts and unpacked directories ARE ignored:
build/                 # Build artifacts
dist/pyinstaller/Radio Monitor/  # Unpacked Windows build directory (only commit .zip files)
dist/pyinstaller/*.zip  # Exception: DO commit the release ZIP files
VERSION.py             # Auto-generated version file (created by pyinstaller/build.py)

# ------------------------------------------
# Documentation (All documentation excluded - keep out of git until ready)
# ------------------------------------------
*.md
docs/                  # Entire docs directory excluded
!linux/**/*.md          # Allow .md files in linux/ subdirectory
!docker/**/*.md         # Allow .md files in docker/ subdirectory
!pyinstaller/**/*.md    # Allow .md files in pyinstaller/ subdirectory

# User mappings documentation (allow these specific files)
!README.md
!INSTALL.md
!API.md
!TROUBLESHOOTING.md
!ARCHITECTURE.md
!USER_MAPPINGS_README.md
!USER_MAPPINGS_QUICKSTART.md

# ------------------------------------------
# Development Tools (Playwright - for design/debugging only)
# ------------------------------------------
# Playwright browser binaries (~350MB) - never commit these
ms-playwright/          # Windows Playwright binaries
.playwright/            # Unix/Mac Playwright binaries
playwright-screenshots/ # Screenshots captured during development
playwright-*            # Playwright cache and report files
playwright_helper.py    # Playwright helper script

# ------------------------------------------
# Testing (Development only - not for production)
# ------------------------------------------
tests/                  # Test files and fixtures
pytest.ini              # Pytest configuration (if needed)
.pytest_cache/          # Pytest cache directory

# ------------------------------------------
# Development Dependencies
# ------------------------------------------
requirements-dev.txt    # Development-only dependencies
requirements.*.txt      # Any environment-specific requirements
!requirements.txt       # Exception: keep main requirements.txt

# ------------------------------------------
# Database Migrations (Temporary scripts, not for production)
# ------------------------------------------
migrations/             # Database migration scripts (one-time use)

# ------------------------------------------
# Screenshots and Debug Images
# ------------------------------------------
*_screenshot.png        # Any screenshot files
screenshot*.png         # Screenshot files with different pattern
*.screenshot.png        # Screenshot files with different pattern
